{%- assign hero_bg = section.settings.bg_image -%}

<section class="lp-b2b-livery">

  <div class="lp-b2b-livery__hero">
    {%- if hero_bg != blank -%}
      {{ hero_bg | image_url: width: 2000 | image_tag:
         class: 'lp-b2b-livery__bg',
         loading: 'lazy',
         widths: '800,1200,1600,2000',
         sizes: '100vw',
         alt: hero_bg.alt | default: section.settings.heading }}
    {%- endif -%}
    <div class="lp-b2b-livery__hero-overlay">
      {%- if section.settings.heading != blank -%}
        <h2 class="lp-b2b-livery__heading">{{ section.settings.heading | newline_to_br }}</h2>
      {%- endif -%}
    </div>
  </div>

  {%- if section.blocks.size > 0 -%}
    <div class="lp-b2b-livery__cards">
      {%- for block in section.blocks -%}
        {%- if block.type == 'livery' -%}
          {%- assign card_link = block.settings.link -%}
          <article class="lp-b2b-livery__card" {{ block.shopify_attributes }}>

            <div class="lp-b2b-livery__card-text">
              {%- if block.settings.number != blank -%}
                <span
                  class="lp-b2b-livery__badge"
                  style="background-color: {{ block.settings.badge_bg | default: '#E8B82E' }}; color: {{ block.settings.badge_text | default: '#111111' }};"
                >{{ block.settings.number }}</span>
              {%- endif -%}
              {%- if block.settings.title != blank -%}
                <h3 class="lp-b2b-livery__title">{{ block.settings.title | newline_to_br }}</h3>
              {%- endif -%}
              <span class="lp-b2b-livery__rule" aria-hidden="true"></span>
              {%- if block.settings.text != blank -%}
                <p class="lp-b2b-livery__text">{{ block.settings.text | newline_to_br }}</p>
              {%- endif -%}
              {%- if block.settings.color_1 != blank or block.settings.color_2 != blank -%}
                <div class="lp-b2b-livery__swatches" aria-hidden="true">
                  {%- if block.settings.color_1 != blank -%}
                    <span class="lp-b2b-livery__swatch" style="background-color: {{ block.settings.color_1 }};"></span>
                  {%- endif -%}
                  {%- if block.settings.color_2 != blank -%}
                    <span class="lp-b2b-livery__swatch" style="background-color: {{ block.settings.color_2 }};"></span>
                  {%- endif -%}
                </div>
              {%- endif -%}
            </div>

            {%- if block.settings.image != blank -%}
              <div class="lp-b2b-livery__media">
                {{ block.settings.image | image_url: width: 1400 | image_tag:
                   class: 'lp-b2b-livery__img',
                   loading: 'lazy',
                   widths: '600,900,1200,1400',
                   sizes: '(min-width:1100px) 55vw, 100vw',
                   alt: block.settings.image.alt | default: block.settings.title }}
              </div>
            {%- endif -%}

            {%- if block.settings.image != blank -%}
              <button
                type="button"
                class="lp-b2b-livery__arrow"
                data-livery-zoom
                data-zoom-src="{{ block.settings.image | image_url: width: 2400 }}"
                data-zoom-alt="{{ block.settings.image.alt | default: block.settings.title | escape }}"
                aria-label="{{ 'Ingrandisci ' | append: block.settings.title | default: 'Ingrandisci livrea' }}"
              >
                <svg viewBox="0 0 24 24" fill="none" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                  <path d="m9 6 6 6-6 6"/>
                </svg>
              </button>
            {%- else -%}
              <span class="lp-b2b-livery__arrow lp-b2b-livery__arrow--static" aria-hidden="true">
                <svg viewBox="0 0 24 24" fill="none" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round">
                  <path d="m9 6 6 6-6 6"/>
                </svg>
              </span>
            {%- endif -%}

          </article>
        {%- endif -%}
      {%- endfor -%}
    </div>
  {%- endif -%}

  <div class="lp-b2b-livery__zoom" id="lpLiveryZoom" aria-hidden="true" role="dialog" aria-modal="true" aria-label="Dettaglio livrea">
    <div class="lp-b2b-livery__zoom-overlay" data-zoom-close></div>
    <div class="lp-b2b-livery__zoom-dialog" role="document">
      <button type="button" class="lp-b2b-livery__zoom-close" data-zoom-close aria-label="Chiudi">
        <svg viewBox="0 0 24 24" fill="none" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M18 6 6 18M6 6l12 12"/></svg>
      </button>
      <div class="lp-b2b-livery__zoom-frame" data-zoom-frame></div>
    </div>
  </div>

</section>

{% stylesheet %}

.lp-b2b-livery {
  --b2b-gold:        #E8B82E;
  --b2b-gold-light:  #FFD84D;
  --b2b-gold-dark:   #B8881A;
  --b2b-gold-faint:  rgba(232, 184, 46, 0.25);
  background: #000;
  color: #fff;
}

/* ── Hero band (bg + headline) ── */
.lp-b2b-livery__hero {
  position: relative;
  width: 100%;
  aspect-ratio: 16 / 9;
  min-height: 260px;
  overflow: hidden;
  background: #0a0a0a;
}

.lp-b2b-livery__bg {
  position: absolute;
  inset: 0;
  width: 100%;
  height: 100%;
  object-fit: cover;
  object-position: center;
  display: block;
  z-index: 0;
}

.lp-b2b-livery__hero::after {
  content: "";
  position: absolute;
  inset: 0;
  z-index: 1;
  pointer-events: none;
  background:
    linear-gradient(to bottom,
      rgba(0,0,0,0.5) 0%,
      rgba(0,0,0,0.15) 25%,
      rgba(0,0,0,0) 45%,
      rgba(0,0,0,0.4) 70%,
      rgba(0,0,0,0.85) 88%,
      #000 100%);
}

.lp-b2b-livery__hero-overlay {
  position: relative;
  z-index: 2;
  height: 100%;
  display: flex;
  align-items: flex-start;
  justify-content: flex-end;
  padding: 24px 20px;
}

.lp-b2b-livery__heading {
  font-family: 'Archivo Black', 'Inter', sans-serif;
  font-size: 1.7rem;
  line-height: 0.95;
  letter-spacing: -0.01em;
  color: #fff;
  margin: 0;
  text-transform: uppercase;
  max-width: 14ch;
  text-align: right;
}

/* ── Cards stack ── */
.lp-b2b-livery__cards {
  position: relative;
  z-index: 2;
  display: flex;
  flex-direction: column;
  gap: 12px;
  padding: 0 12px;
  margin-top: -56px;
}

.lp-b2b-livery__card {
  position: relative;
  display: grid;
  grid-template-columns: 1fr;
  grid-template-areas:
    "text"
    "media";
  padding: 14px 16px 16px;
  border: 1px solid var(--b2b-gold-faint);
  border-radius: 12px;
  background: #000;
  gap: 8px;
}

.lp-b2b-livery__card-text {
  grid-area: text;
  display: flex;
  flex-direction: column;
  align-items: flex-start;
  gap: 6px;
}

.lp-b2b-livery__badge {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  font-family: 'Archivo Black', 'Inter', sans-serif;
  font-size: 0.78rem;
  font-weight: 900;
  letter-spacing: 0.04em;
  padding: 4px 10px;
  border-radius: 4px;
  line-height: 1;
}

.lp-b2b-livery__title {
  font-family: 'Archivo Black', 'Inter', sans-serif;
  font-size: 1.2rem;
  line-height: 1;
  color: #fff;
  margin: 0;
  text-transform: uppercase;
  letter-spacing: 0;
}

.lp-b2b-livery__rule {
  display: block;
  width: 56px;
  height: 1.5px;
  background: var(--b2b-gold);
  margin: 1px 0 2px;
}

.lp-b2b-livery__text {
  font-family: 'Inter', sans-serif;
  font-size: 0.85rem;
  font-weight: 500;
  line-height: 1.3;
  color: #fff;
  margin: 0;
  max-width: 38ch;
}

.lp-b2b-livery__swatches {
  display: flex;
  gap: 5px;
  margin-top: 2px;
}

.lp-b2b-livery__swatch {
  display: block;
  width: 38px;
  height: 14px;
  border-radius: 3px;
  border: 1px solid rgba(255,255,255,0.35);
}

/* ── Media (truck + shoe combo image) — zoomed ── */
.lp-b2b-livery__media {
  grid-area: media;
  position: relative;
  width: 100%;
  aspect-ratio: 16 / 6;
  overflow: hidden;
}

.lp-b2b-livery__img {
  width: 100%;
  height: 100%;
  object-fit: contain;
  object-position: center;
  display: block;
}

/* ── Arrow button ── */
.lp-b2b-livery__arrow {
  position: absolute;
  top: 18px;
  right: 18px;
  width: 44px;
  height: 44px;
  border: 1.5px solid var(--b2b-gold);
  border-radius: 50%;
  background: rgba(0,0,0,0.4);
  display: inline-flex;
  align-items: center;
  justify-content: center;
  text-decoration: none;
  flex-shrink: 0;
  transition: background 0.15s, transform 0.15s;
}
.lp-b2b-livery__arrow:hover {
  background: rgba(232, 184, 46, 0.15);
  text-decoration: none;
}
.lp-b2b-livery__arrow:active {
  transform: scale(0.96);
}
.lp-b2b-livery__arrow svg {
  width: 20px;
  height: 20px;
  stroke: var(--b2b-gold);
}
.lp-b2b-livery__arrow--static { pointer-events: none; }

/* ── Tablet ≥ 750px ── */
@media (min-width: 750px) {
  .lp-b2b-livery__hero {
    aspect-ratio: 21 / 9;
    min-height: 340px;
  }
  .lp-b2b-livery__hero-overlay { padding: 40px 48px; }
  .lp-b2b-livery__heading {
    font-size: 2.6rem;
    max-width: 18ch;
  }

  .lp-b2b-livery__cards {
    margin-top: -72px;
    padding: 0 32px;
    gap: 16px;
  }

  .lp-b2b-livery__card {
    grid-template-columns: minmax(200px, 1fr) 1.8fr;
    grid-template-areas: "text media";
    align-items: center;
    padding: 18px 32px;
    gap: 20px;
    border-radius: 14px;
  }

  .lp-b2b-livery__title { font-size: 1.5rem; }
  .lp-b2b-livery__text { font-size: 0.95rem; }
  .lp-b2b-livery__rule { width: 72px; }
  .lp-b2b-livery__swatch { width: 46px; height: 18px; }

  .lp-b2b-livery__media {
    aspect-ratio: 16 / 6;
  }

  .lp-b2b-livery__arrow {
    position: absolute;
    top: 50%;
    right: 24px;
    transform: translateY(-50%);
    width: 46px;
    height: 46px;
  }
  .lp-b2b-livery__arrow svg { width: 22px; height: 22px; }
}

/* ── Desktop ≥ 1100px ── */
@media (min-width: 1100px) {
  .lp-b2b-livery__hero {
    aspect-ratio: 21 / 7;
    min-height: 420px;
  }
  .lp-b2b-livery__hero-overlay { padding: 56px 64px; }
  .lp-b2b-livery__heading {
    font-size: 3.8rem;
    max-width: 22ch;
  }

  .lp-b2b-livery__cards {
    margin-top: -96px;
    padding: 0 48px;
    gap: 20px;
  }

  .lp-b2b-livery__card {
    grid-template-columns: minmax(260px, 1fr) 2fr 72px;
    grid-template-areas: "text media arrow";
    padding: 24px 40px;
    gap: 32px;
    border-radius: 16px;
  }

  .lp-b2b-livery__title { font-size: 1.8rem; }
  .lp-b2b-livery__text { font-size: 1rem; }
  .lp-b2b-livery__rule { width: 84px; height: 2px; }
  .lp-b2b-livery__badge { font-size: 0.82rem; padding: 4px 11px; }
  .lp-b2b-livery__swatch { width: 54px; height: 20px; }

  .lp-b2b-livery__media {
    aspect-ratio: 16 / 6;
  }

  .lp-b2b-livery__arrow {
    position: static;
    transform: none;
    grid-area: arrow;
    justify-self: center;
    align-self: center;
    width: 56px;
    height: 56px;
  }
  .lp-b2b-livery__arrow svg { width: 24px; height: 24px; }
}

/* ── Zoom modal (right-half of livery image) ── */
.lp-b2b-livery__arrow {
  -webkit-appearance: none;
  appearance: none;
  cursor: pointer;
  padding: 0;
  font: inherit;
  color: inherit;
}

.lp-b2b-livery__zoom {
  position: fixed;
  inset: 0;
  z-index: 9999;
  display: none;
  align-items: center;
  justify-content: center;
  padding: 16px;
}
.lp-b2b-livery__zoom.is-open { display: flex; }

.lp-b2b-livery__zoom-overlay {
  position: absolute;
  inset: 0;
  background: rgba(0, 0, 0, 0.82);
  backdrop-filter: blur(6px);
  -webkit-backdrop-filter: blur(6px);
  cursor: zoom-out;
}

.lp-b2b-livery__zoom-dialog {
  position: relative;
  width: 100%;
  max-width: 1100px;
  max-height: 92vh;
  background: #0a0a0a;
  border: 1px solid var(--b2b-gold-faint);
  border-radius: 14px;
  overflow: hidden;
  display: flex;
  flex-direction: column;
}

.lp-b2b-livery__zoom-close {
  position: absolute;
  top: 12px;
  right: 12px;
  z-index: 2;
  width: 42px;
  height: 42px;
  border-radius: 50%;
  border: 1.5px solid var(--b2b-gold);
  background: rgba(0, 0, 0, 0.55);
  display: inline-flex;
  align-items: center;
  justify-content: center;
  cursor: pointer;
  -webkit-appearance: none;
  appearance: none;
  padding: 0;
}
.lp-b2b-livery__zoom-close:hover { background: rgba(232, 184, 46, 0.18); }
.lp-b2b-livery__zoom-close svg { width: 18px; height: 18px; stroke: var(--b2b-gold); }

.lp-b2b-livery__zoom-frame {
  width: 100%;
  aspect-ratio: 4 / 5;
  background-color: #000;
  background-repeat: no-repeat;
  background-position: right center;
  background-size: 200% 100%;
}

@media (min-width: 750px) {
  .lp-b2b-livery__zoom { padding: 32px; }
  .lp-b2b-livery__zoom-close { top: 16px; right: 16px; width: 46px; height: 46px; }
  .lp-b2b-livery__zoom-close svg { width: 20px; height: 20px; }
  .lp-b2b-livery__zoom-frame { aspect-ratio: 16 / 10; }
}

@media (min-width: 1100px) {
  .lp-b2b-livery__zoom-frame { aspect-ratio: 16 / 8; }
}

@media (prefers-reduced-motion: reduce) {
  .lp-b2b-livery__zoom { transition: none; }
}

{% endstylesheet %}

<script>
(function () {
  if (window.__lpB2bLiveryZoomInit) return;
  window.__lpB2bLiveryZoomInit = true;

  function ready(fn) {
    if (document.readyState === 'loading') document.addEventListener('DOMContentLoaded', fn);
    else fn();
  }

  ready(function () {
    const modal = document.getElementById('lpLiveryZoom');
    if (!modal) return;
    const frame = modal.querySelector('[data-zoom-frame]');
    let lastFocused = null;

    function open(src, alt) {
      if (!src) return;
      frame.style.backgroundImage = 'url("' + src.replace(/"/g, '\\"') + '")';
      frame.setAttribute('role', 'img');
      frame.setAttribute('aria-label', alt || 'Dettaglio livrea');
      modal.classList.add('is-open');
      modal.setAttribute('aria-hidden', 'false');
      document.body.style.overflow = 'hidden';
      lastFocused = document.activeElement;
      const closeBtn = modal.querySelector('[data-zoom-close].lp-b2b-livery__zoom-close');
      if (closeBtn) closeBtn.focus();
    }

    function close() {
      modal.classList.remove('is-open');
      modal.setAttribute('aria-hidden', 'true');
      document.body.style.overflow = '';
      // Defer image clear so transition (if added later) doesn't flash white
      setTimeout(function () {
        if (!modal.classList.contains('is-open')) frame.style.backgroundImage = '';
      }, 50);
      if (lastFocused && typeof lastFocused.focus === 'function') lastFocused.focus();
    }

    document.addEventListener('click', function (e) {
      const trigger = e.target.closest('[data-livery-zoom]');
      if (trigger) {
        e.preventDefault();
        open(trigger.getAttribute('data-zoom-src'), trigger.getAttribute('data-zoom-alt'));
        return;
      }
      if (e.target.closest('[data-zoom-close]') && modal.contains(e.target)) {
        e.preventDefault();
        close();
      }
    });

    document.addEventListener('keydown', function (e) {
      if (e.key === 'Escape' && modal.classList.contains('is-open')) close();
    });
  });
})();
</script>

{% schema %}
{
  "name": "LP B2B — Livrea",
  "settings": [
    {
      "type": "header",
      "content": "Hero band"
    },
    {
      "type": "image_picker",
      "id": "bg_image",
      "label": "Immagine di sfondo (scarpa + ambiente)"
    },
    {
      "type": "textarea",
      "id": "heading",
      "label": "Titolo",
      "default": "LA LIVREA CONTINUA AI PIEDI DEI TUOI AUTISTI."
    }
  ],
  "blocks": [
    {
      "type": "livery",
      "name": "Livrea",
      "limit": 8,
      "settings": [
        {
          "type": "text",
          "id": "number",
          "label": "Numero badge",
          "default": "01"
        },
        {
          "type": "text",
          "id": "title",
          "label": "Titolo livrea"
        },
        {
          "type": "textarea",
          "id": "text",
          "label": "Descrizione"
        },
        {
          "type": "color",
          "id": "color_1",
          "label": "Colore 1",
          "default": "#000000"
        },
        {
          "type": "color",
          "id": "color_2",
          "label": "Colore 2",
          "default": "#E8B82E"
        },
        {
          "type": "color",
          "id": "badge_bg",
          "label": "Sfondo badge numero",
          "default": "#E8B82E"
        },
        {
          "type": "color",
          "id": "badge_text",
          "label": "Testo badge numero",
          "default": "#111111"
        },
        {
          "type": "image_picker",
          "id": "image",
          "label": "Immagine camion + scarpa"
        },
        {
          "type": "url",
          "id": "link",
          "label": "Link CTA (freccia)"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "LP B2B — Livrea",
      "blocks": [
        { "type": "livery", "settings": { "number": "01", "title": "NERO + GIALLO",  "text": "Aggressiva, visibile, stile racing.", "color_1": "#000000", "color_2": "#E8B82E", "badge_bg": "#E8B82E", "badge_text": "#111111" }},
        { "type": "livery", "settings": { "number": "02", "title": "BIANCO + ROSSO", "text": "Classica, pulita, ad alta visibilità.", "color_1": "#FFFFFF", "color_2": "#C0192A", "badge_bg": "#C0192A", "badge_text": "#FFFFFF" }},
        { "type": "livery", "settings": { "number": "03", "title": "BLU + BIANCO",   "text": "Professionale, affidabile, istituzionale.", "color_1": "#1B3D8C", "color_2": "#FFFFFF", "badge_bg": "#1B3D8C", "badge_text": "#FFFFFF" }},
        { "type": "livery", "settings": { "number": "04", "title": "VERDE + ORO",    "text": "Distintiva, ricercata, premium.", "color_1": "#2D5C3E", "color_2": "#C9A227", "badge_bg": "#2D5C3E", "badge_text": "#FFFFFF" }}
      ]
    }
  ]
}
{% endschema %}
